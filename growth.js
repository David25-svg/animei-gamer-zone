const t=document.querySelector('[data-theme-toggle]');if(t)t.onclick=()=>{const h=document.documentElement;h.dataset.theme=h.dataset.theme==='dark'?'light':'dark';};const f=document.getElementById('leadForm');if(f)f.addEventListener('submit',e=>{e.preventDefault();const d=new FormData(f);const services=d.getAll('services').join(', ')||'Not specified';const lines=[['Name',d.get('name')],['Company',d.get('company')],['Email',d.get('email')],['Company country / region',d.get('country')],['Team traveling from',d.get('origin')],['Event / Convention',d.get('event')],['Event dates',d.get('dates')],['Booth / activation size',d.get('booth')],['Demo stations',d.get('stations')],['On-site staff needed',d.get('staff')],['Services needed',services],['Estimated support budget',d.get('budget')],['What they need help with',d.get('notes')]].filter(x=>x[1]);const subject=`U.S. Event Support Request — ${d.get('company')||d.get('name')||'New Lead'}`;const body=lines.map(x=>`${x[0]}: ${x[1]}`).join('
');location.href=`mailto:support@animegamerzone.com?subject=${encodeURIComponent(subject)}&body=${encodeURIComponent(body)}`;});